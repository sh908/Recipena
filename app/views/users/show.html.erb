<div class = "container">
 <div class = "row">
  <div class = "col-md-8 offset-md-1">

    <h1>マイページ</h1>
      <div class = "profile-container">
       <h3 class = "profile-name"><%= @user.last_name %><%= @user.first_name %></h3>
      
        <!--attachment_image_tag 保存先インスタンス名, :保存先カラム名, :サイズ（横幅,高さ）, 画像拡張子, 代替画像 -->
        <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no-image.jpeg" %>
    
        <!--自分（ログインしているユーザ）のマイページが表示されたときのみ、編集ページへのリンクが表示される-->
        <% if @user.id == current_user.id %>
         <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
        <% end %>
      </div>
    <% @posts.each do |post| %>
      <div>
        <%= link_to post_path(post.id)do %>
         <%= attachment_image_tag post, :image %>
        <% end %>
        <p>投稿ユーザー画像<%= attachment_image_tag post.user, :profile_image, :fill, 60, 60, fallback: "no-image.jpeg" %></p>
        <p>ユーザーネーム<%= post.user.last_name %><%= post.user.first_name %></p>
        <p>料理名<%= post.cook_name %></p>
        <p>説明<%= post.description %></p>
        <p><%= link_to "#{post.post_comments.count}コメント", post_path(post.id) %></p>
      </div>
    <% end %>
    <%= paginate @posts %>
  </div>
 </div>
</div>